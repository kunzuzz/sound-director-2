{
  "name": "sound-director",
  "version": "1.0.0",
  "description": "A full-stack application for theatre sound designers",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "server": "PORT=5001 nodemon server.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "start": "node server.js"
  },
  "keywords": ["sound", "theatre", "music", "design"],
  "author": "Sound Director",
  "license": "MIT",
 "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}
